---
import PageLayout from '@/layouts/PageLayout.astro'
import { Prose } from "@/components/starwind/prose";

const { frontmatter } = Astro.props
---

<PageLayout width="narrow" title={frontmatter.title} subtitle={frontmatter.subtitle}>
  <Prose>
    <slot />
  </Prose>
</PageLayout>
